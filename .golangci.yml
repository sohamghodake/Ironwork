version: "2"

run:
  timeout: 5m

linters:
  # default set (errcheck, govet, ineffassign, staticcheck, unused) plus:
  enable:
    - misspell
    - unconvert
    - unparam
  exclusions:
    paths:
      - gen

formatters:
  enable:
    - gofmt
    - goimports
  exclusions:
    paths:
      - gen
